PORT=8787
CORS_ORIGIN=http://localhost:8000

# Optional simple write protection. If set, write requests must send x-app-key.
APP_WRITE_KEY=

# 可选的 Supabase 配置。若不填写，后端会继续使用内存模式。
# 注意：这里必须使用 service_role key，不要使用 anon key。
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
