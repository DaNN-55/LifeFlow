# LifeFlow 长期架构方案

更新日期：2026-03-22

## 目标

这套方案面向两个前提：

- 这个网站会长期持续使用
- 后续有机会开放给其他人使用

因此目标不是“先能跑”，而是：

- 长期稳定
- 数据可持续保存
- 后续可扩展
- 面向中国大陆访问体验更合理

## 当前项目现状

前端：

- 当前是原生 `HTML + CSS + 模块化 JavaScript`
- 已部署/可部署到 `Vercel`
- 页面和交互已经比较复杂，包含 `Today / Weekly / Finance / Science / AI / 账号 / 同步中心 / 设置`

后端：

- 当前是 `Node.js + Express`
- 存储层目前只有两种：
  - `Supabase`
  - `memory`

当前代码中，不接 Supabase 就会退回 `memory` 模式。  
这意味着如果服务重启，数据无法持久保留，所以不适合作为长期正式方案。

## 推荐长期架构

### 架构结论

推荐采用：

1. 前端：短期继续放在 `Vercel`
2. 后端：迁到国内云服务器或容器
3. 数据库：国内托管 `PostgreSQL`
4. 文件与备份：国内对象存储（`COS` / `OSS`）
5. 长期：如果未来主要面向中国大陆用户，再把前端静态资源也迁到国内 CDN / 静态托管

### 为什么这样选

#### 1. 前端继续保留 Vercel

短期内保留 Vercel 有这些优点：

- 不用立刻重做前端部署链路
- 静态资源分发简单
- 可以把精力优先放到后端与数据迁移

但要注意：

- Vercel 更适合静态站点和靠近海外区域的访问
- 如果将来主要服务中国大陆用户，前端静态资源长期也建议迁到国内 CDN

#### 2. 后端迁到国内

原因：

- 当前 API、登录、同步、内容抓取都是核心业务
- 如果数据库在国内，而后端还在海外，时延会比较差
- 如果未来用户主要在中国大陆，后端放国内更稳

#### 3. 数据库选 PostgreSQL

这是当前最合适的长期方案。

原因：

- 你现在的数据天然是关系型数据
- 后续用户、任务、周总结、RSS 信源、收藏、内容缓存都适合关系型建模
- 比 SQLite 更适合后期开放给别人使用
- 比继续依赖 Supabase 更可控

推荐优先级：

1. 国内托管 PostgreSQL
2. 自建 PostgreSQL
3. SQLite（仅适合个人单机早期，不推荐长期）

## 推荐云资源组合

### 方案 A：腾讯云

- 云服务器 / 容器：腾讯云
- 数据库：`TencentDB for PostgreSQL`
- 对象存储：`COS`
- CDN / 域名 / HTTPS：腾讯云配套

适合：

- 想统一在一个平台管理
- 面向中国大陆访问

### 方案 B：阿里云

- 云服务器 / 容器：阿里云
- 数据库：`ApsaraDB RDS for PostgreSQL`
- 对象存储：`OSS`
- CDN / 域名 / HTTPS：阿里云配套

适合：

- 更偏国内 Web 基础设施统一化
- 后续如果要做备案、CDN、静态托管，会比较顺

## 对现有项目的具体影响

### 1. 前端改动量

前端不需要大改。

主要只要改这些：

- 默认 API 地址
- 登录页 API 地址
- CORS 对应的新域名

换句话说：

- 页面 UI 基本不用因为迁云而重写
- 前端改动量小

### 2. 后端改动量

后端属于中等改动量，不是小修，但也不是重做。

核心工作：

1. 新增一个 `PostgresStore`
2. 让后端支持通过配置切换到 PostgreSQL
3. 保持现有 API 不变

这样前端就几乎不用感知后端底层已经换掉。

### 3. 必须新增的能力

为了长期使用，建议顺手补这些：

- 数据库备份策略
- 日志与错误监控
- RSS 抓取失败重试
- 定期清理历史内容缓存
- 登录安全与限流

## 数据迁移方案

你现在的迁移要分成两层理解。

### 第一层：核心业务数据

你现在同步中心已经支持导入 / 导出 JSON。

这部分可以迁移：

- 任务
- 每日记录
- 周总结
- 偏好设置

所以如果只迁核心个人数据，路径很简单：

1. 在旧站导出 JSON
2. 新后端上线
3. 登录新账号
4. 在新站导入 JSON

### 第二层：全量云端数据

如果你要保留真正“全部历史数据”，现有导出还不够。

还需要迁移这些云端表数据：

- 用户账号
- 会话
- RSS 信源
- 内容缓存
- 内容收藏

因此长期正确做法是：

1. 先导出一份前端 JSON，保核心数据
2. 再写一个一次性迁移脚本，把 Supabase 中的表全量搬到新 PostgreSQL

## 最合理的实施顺序

### 阶段 1：基础设施落地

目标：

- 国内后端上线
- PostgreSQL 上线
- 对象存储准备好

这一步先不动前端框架。

### 阶段 2：后端存储迁移

目标：

- 新增 `PostgresStore`
- API 保持不变
- 用 PostgreSQL 替代 Supabase

### 阶段 3：数据迁移

目标：

- 导入现有核心数据
- 执行 Supabase 全量迁移脚本
- 校验任务、周总结、RSS 信源、收藏是否完整

### 阶段 4：运营级稳定性补强

目标：

- 备份
- 监控
- 日志
- 抓取任务治理
- 内容表清理

### 阶段 5：视用户分布决定是否迁前端静态资源

如果后面主要是中国大陆用户，再把前端从 Vercel 迁到国内静态托管 / CDN。

## 最终建议

从长期角度看，推荐你采用下面这套最终形态：

- 前端：先保留 Vercel
- 后端：国内 Node + Express
- 数据库：国内 PostgreSQL
- 文件存储：COS 或 OSS
- 数据迁移：前端 JSON 导出/导入 + 一次性 Supabase 全量迁移脚本

这套方案的优点是：

- 风险可控
- 不需要立刻推倒前端
- 数据能长期稳定保存
- 后续开放给别人时也有扩展空间

## 面向 PWA / App 的长期考虑

你后续不仅会长期维护网站，还有计划做：

- `PWA`
- 原生 App

这会让长期架构判断更明确。

### 核心思路

未来应该逐步演进成：

> 一个统一后端，服务 Web / PWA / 原生 App 三端

也就是说：

- 网页前端只是一个入口
- PWA 只是 Web 的增强形态
- 原生 App 以后也调用同一套 API
- 真正重要的是后端 API 和数据库层稳定

### 这对当前架构意味着什么

这意味着未来最不应该继续绑定死的，是：

- 特定前端部署平台
- 特定 BaaS 数据平台

最应该稳定下来的，是：

- 自己可控的后端
- 自己可控的数据库
- 统一的数据模型
- 统一的认证体系

### 对后续 App 友好的架构要求

如果后面要兼容 PWA 或原生 App，建议提前按这些原则规划：

1. API 继续保持后端中心化

- 登录
- 任务
- 每日记录
- 周总结
- RSS 信源
- 收藏 / 已读
- 同步

这些都应继续走后端，而不是放在前端本地逻辑里。

2. 后端接口保持稳定

长期建议逐步过渡到：

- `/api/v1/...`

这样未来 Web、PWA、原生 App 可以统一接入。

3. 认证机制要为 App 预留演进空间

当前 Web 用 `cookie + x-session-id` 能工作。  
但如果后续做原生 App，长期更推荐演进到：

- access token
- refresh token

短期不需要立刻改，但架构上要预留这条路。

4. 文件与备份要独立

以后如果做 App，可能会出现：

- 头像
- 附件
- 截图
- 导出文件
- 备份文件

因此对象存储是长期值得保留的一环。

## 只买数据库是否足够

结论：`不够`

因为你现在的网站不是纯静态站点。  
除了前端页面以外，后端还承担这些职责：

- 登录鉴权
- 同步中心
- 任务 / 周总结 API
- RSS 抓取与刷新
- 天气代理
- 内容源管理

所以如果前端继续放在 Vercel，国内至少还需要：

1. 一台服务器或容器实例
2. 一个数据库

### 最小长期可用组合

推荐至少购买：

- `1 台服务器`
- `1 个托管 PostgreSQL`

如果只买数据库，会缺失后端运行环境。  
除非你打算用自己的本地机器长期运行后端，但这不适合作为正式长期方案。

## 阿里云 / 腾讯云采购建议

### 长期推荐购买方式

推荐两种组合：

#### 方案 1：最低可用长期方案

- `2核2G` 服务器
- `PostgreSQL 基础实例`

适合：

- 当前个人长期使用
- 成本控制优先
- 未来仍保留扩展空间

#### 方案 2：更稳一点的长期方案

- `2核4G` 服务器
- `PostgreSQL 基础或高可用实例`

适合：

- RSS 抓取频率更高
- 后续准备接更多 API
- 未来有开放给他人的可能

### 腾讯云建议

推荐优先关注：

- 轻量应用服务器
- TencentDB for PostgreSQL

官方入口：

- 轻量应用服务器价格页  
  https://cloud.tencent.com/product/lighthouse/pricing

- 云服务器 CVM  
  https://cloud.tencent.com/product/cvm

- TencentDB for PostgreSQL  
  https://cloud.tencent.com/product/postgres

- PostgreSQL 购买方式  
  https://cloud.tencent.com/document/product/409/7550

- PostgreSQL 计费概述  
  https://cloud.tencent.com/document/product/409/4993

根据官方页面当前展示，腾讯云轻量服务器常见入门规格大致有：

- `2核2G / 40GB / 3Mbps`：`45元/月`，`459元/年`
- `2核2G / 50GB / 4Mbps`：`50元/月`，`510元/年`
- `2核4G / 100GB / 7Mbps`：`100元/月`，`1020元/年`

这说明腾讯云对你这种项目来说，前期采购门槛不高。

### 阿里云建议

推荐优先关注：

- 轻量应用服务器
- ECS
- ApsaraDB RDS for PostgreSQL

官方入口：

- 轻量应用服务器  
  https://www.aliyun.com/product/swas

- ECS  
  https://www.aliyun.com/product/ecs

- ApsaraDB RDS for PostgreSQL  
  https://www.alibabacloud.com/product/apsaradb-for-rds-postgresql

- PostgreSQL 计费说明  
  https://www.alibabacloud.com/help/en/rds/apsaradb-rds-for-postgresql/billable-items-billing-methods-and-pricing

阿里云官方更偏向让用户进入购买页查看实时价格，因此最终采购价格以控制台为准。

### 两个平台怎么选

#### 如果你更看重

- 入门成本清晰
- 轻量服务器直观
- 先快速搭起来

更建议先看 `腾讯云`

#### 如果你更看重

- 国内基础设施完整
- 后续备案、CDN、静态托管体系更统一
- 长期更偏企业化

更建议先看 `阿里云`

## 方案 A / 方案 B 成本测算

更新口径：2026-03-22

说明：

- 以下成本优先按“免费优先”原则估算
- 前端继续使用 `Vercel Hobby`
- 免费 SSL 优先
- 对象存储先不强制购买
- 不包含域名费用
- 不包含备案相关成本
- 不包含未来短信、推送、监控等增值成本

### 统一前提

默认按以下组合估算：

- 前端：`Vercel Hobby`
- HTTPS：免费证书
- 对象存储：先按 `0 元起`
- 服务器：轻量应用服务器优先
- 数据库：PostgreSQL

### 方案 A：腾讯云成本测算

#### A1. 最低可运行方案

组成：

- 前端：`Vercel Hobby`
- 后端：腾讯云 `Lighthouse 2核2G 40GB 3Mbps`
- 数据库：先自建 PostgreSQL
- SSL：免费

成本：

- `Vercel Hobby`：`0 元`
- 腾讯云轻量服务器：`45 元/月` 或 `459 元/年`
- SSL：`0 元`
- COS：`0 元起`

合计：

- 首月：理论上可接近 `0 元`
  - 腾讯云轻量页面当前有“新用户免费体验 1 个月”入口
- 第一年的稳定成本：`459 元/年`

适合：

- 先把后端从 Supabase 迁走
- 成本控制优先
- 暂时接受数据库自建

#### A2. 更稳一点的方案

组成：

- 前端：`Vercel Hobby`
- 后端：腾讯云 `Lighthouse 2核4G 100GB 7Mbps`
- 数据库：先自建 PostgreSQL
- SSL：免费

成本：

- 轻量服务器：`100 元/月` 或 `1020 元/年`
- 其他部分：`0 元起`

合计：

- 第一年的稳定成本：`1020 元/年`

适合：

- RSS 抓取更频繁
- 后端服务更稳
- 后续准备承载更多功能

#### A3. 托管数据库方案

组成：

- 前端：`Vercel Hobby`
- 后端：腾讯云轻量服务器
- 数据库：`TencentDB for PostgreSQL`

当前能确认的官方信息：

- 腾讯云 PostgreSQL 明确按这些计费：
  - 规格费用
  - 存储空间费用
  - 超额备份费用
  - 日志目前免费
  - 流量目前免费

但我这次没有从腾讯云公开产品页直接拿到低配实例的固定价格数字。  
因此这部分更适合按“服务器费用 + 控制台实时数据库价格”来判断。

建议预算方式：

- `最低预算参考`：`459 元/年 + PostgreSQL 控制台报价`
- `更稳预算参考`：`1020 元/年 + PostgreSQL 控制台报价`

#### 腾讯云相关链接

- 轻量应用服务器价格页  
  https://cloud.tencent.com/product/lighthouse/pricing

- 腾讯云 PostgreSQL 产品页  
  https://cloud.tencent.com/product/postgres

- 腾讯云 PostgreSQL 计费概述  
  https://cloud.tencent.com/document/product/409/4993

- 腾讯云 PostgreSQL 购买方式  
  https://cloud.tencent.com/document/product/409/7550

- 腾讯云 SSL  
  https://cloud.tencent.com/product/ssl/

- 腾讯云 COS  
  https://intl.cloud.tencent.com/product/cos

### 方案 B：阿里云成本测算

说明：

- 阿里云轻量服务器与部分 PostgreSQL 低价数字，这次主要来自阿里云开发者社区对官方购买页的价格快照
- 最终购买价格请以阿里云购买页实时显示为准

#### B1. 最低可运行方案

组成：

- 前端：`Vercel Hobby`
- 后端：阿里云轻量应用服务器 `2核2G 3M 50GB`
- 数据库：先自建 PostgreSQL
- SSL：免费

成本：

- `Vercel Hobby`：`0 元`
- 阿里云轻量服务器：`108 元/年`
- SSL：`0 元`
- OSS：`0 元起`

合计：

- 第一年的稳定成本：`108 元/年`

适合：

- 极度控制预算
- 先完成后端迁移
- 暂时接受数据库自建

#### B2. 最平衡的长期方案

组成：

- 前端：`Vercel Hobby`
- 后端：阿里云轻量应用服务器 `2核2G`
- 数据库：阿里云 `RDS PostgreSQL 2核4G 100GB`

参考成本：

- 服务器：`108 元/年`
- RDS PostgreSQL：`227.99 元/年`

合计：

- `335.99 元/年`

适合：

- 长期个人使用
- 后续准备继续做大
- 希望数据库是托管的，不想自己维护

#### B3. 更稳一点的长期方案

组成：

- 前端：`Vercel Hobby`
- 后端：阿里云轻量应用服务器 `2核4G 4M`
- 数据库：阿里云 `RDS PostgreSQL 2核4G 100GB`

参考成本：

- 服务器：`297.98 元/年`
- RDS PostgreSQL：`227.99 元/年`

合计：

- `525.97 元/年`

适合：

- RSS 抓取更频繁
- 后端更稳
- 后续可能开放给别人使用

#### 阿里云相关链接

- 阿里云轻量应用服务器  
  https://www.aliyun.com/product/swas

- 阿里云 ECS  
  https://www.aliyun.com/product/ecs

- 阿里云 PostgreSQL  
  https://www.alibabacloud.com/product/apsaradb-for-rds-postgresql

- 阿里云 PostgreSQL 计费文档  
  https://www.alibabacloud.com/help/en/rds/apsaradb-rds-for-postgresql/billable-items-billing-methods-and-pricing

- 阿里云 OSS  
  https://www.alibabacloud.com/help/en/oss/user-guide/product-introduction/

- 阿里云轻量服务器价格快照参考  
  https://developer.aliyun.com/article/1259092

- 阿里云 RDS PostgreSQL 价格快照参考  
  https://developer.aliyun.com/article/1691218

### 成本对比结论

按当前能核到的信息：

#### 如果你优先考虑最低成本

更便宜的是：

- `方案 B1（阿里云轻量 + 自建 PostgreSQL）`
- 参考成本：`108 元/年`

但这不是最推荐的长期形态，因为数据库还是自建。

#### 如果你优先考虑长期平衡

更推荐的是：

- `方案 B2（阿里云轻量 + 托管 PostgreSQL）`
- 参考成本：`335.99 元/年`

这是当前最值得优先考虑的低成本长期方案。

#### 如果你优先考虑平台直观和轻量产品线清晰

可以优先看：

- `方案 A（腾讯云）`

但腾讯云 PostgreSQL 的低配固定价格，建议登录控制台后再确认。

## 实施上的最终推荐方案

### 当前最推荐的现实方案

短期：

- 前端继续保留在 `Vercel`
- 后端迁到国内
- 数据库换成国内 `PostgreSQL`
- 文件备份放 `COS / OSS`

长期：

- 如果未来主要服务中国大陆用户，再把前端静态资源迁到国内 CDN / 静态托管
- 如果后续做 PWA / 原生 App，继续复用同一套后端 API

### 为什么这是当前最合适的平衡点

因为它同时满足：

- 当前改动可控
- 长期数据可持续
- 兼容未来 PWA
- 兼容未来原生 App
- 不需要立刻重做前端

## 下一步可执行决策

建议按这个顺序做判断：

1. 先选云平台：`腾讯云` 或 `阿里云`
2. 再定数据库：`PostgreSQL`
3. 再决定服务器规格：`2核2G` 还是 `2核4G`
4. 再开始做后端存储层替换
5. 然后执行数据迁移

## 当前最终结论

一句话概括：

> 这套项目的长期正确方向，不是继续依赖 Supabase，也不是只买数据库，更不是回到本地内存模式；而是建立“前端可替换、后端独立、数据库独立、可服务 Web/PWA/App 三端”的国内长期可用架构。

## 当前决策建议

建议先做下面这件事，而不是一次性做所有改动：

1. 先确定云平台（腾讯云或阿里云）
2. 先确定数据库（PostgreSQL）
3. 先完成后端存储层替换
4. 再执行数据迁移

一句话结论：

> 长期最合适的方案不是继续依赖 Supabase，也不是退回本地内存模式，而是把后端和数据库迁到国内，并保留现有前端逐步演进。
